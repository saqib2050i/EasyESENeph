# Copy to .env to set your own login. If you leave AUTH_PASSWORD blank, the server
# auto-generates a strong password on first run and prints it in the logs
# (docker compose logs nephron | grep -i login). Generate a strong one with:
#   python3 -c "import secrets; print(secrets.token_urlsafe(16))"
AUTH_USER=admin
AUTH_PASSWORD=
